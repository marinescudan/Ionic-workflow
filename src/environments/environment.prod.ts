export const environment = {
  production: true,
  // GraphQL endpoints (update with production URLs)
  graphqlEndpoint: 'https://api.production.com/graphql',
  graphqlWsEndpoint: 'wss://api.production.com/graphql',
  // WebRTC Signaling Server (update with deployed URL)
  signalingServerUrl: 'https://your-signaling-server.onrender.com',
  // App server port
  appPort: 8100,
};
